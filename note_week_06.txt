int a;
int *b;
b = &a;

*b b가 가리키는 주소에 들어있는 내용 ex) b = 5,
&a 는 b의 주소값을 의미함 ex) dx34xxxxx...

객체 배열에서는 타입 배열 선언 가능, 
배열을 위한 공간 할당 Circle[3]
Circle[0], Circle[1], Circle[2] 의 생성자 만들기

객체 배열 생성시 기본 생성자가 호출되는데 클래스 선언부? 구현부인듯 한 곳에
기본 생성자가 없을 때 Circle circleArray[3];을 메인에 작성하면 기본 생성자를 삽입해주는데, Circle(int r)을 호출해놓으면 기본 생성자가 없으므로 컴파일 에러
객체 배열 초기화는 [0],[1],[2] 객체가 생성될때 Circle(10) 등을 넣어서 호출

객체 배열을 만들 때 배열도 for문으로 사용이 가능할까?
하다가 포기


동적 메모리 할당 정적 할당-->변수 선언
동적 할당--> 실행 중에(런타임시) 힙 메모리 할당, 런타임이 끝나면 반환을 해야 함 
new 연산자로 힙 메모리로부터 메모리 할당 요청 가능 타입, 배열, 객체, 객체 배열 모두 가능
할당 시 생성자 호출

delete 소멸자 호출 뒤 힙에 반환

p 에 할당해주지 않으면 p값은 null이 아닌 필요하지않은 값으로 채워짐
그래서 결과는 똑같이 나오긴 하는데 메모리 하나 버리는중?

정수형 배열의 동적 할당은 for도 돌리고 while 도 돌리는데 결과물은 비슷하다 cin 하고 sum 할때 음

예제 4-10 
cout 으로 보내고 원의 개수 받고 개수 받은 걸로 개수대로 배열 만든 후 ..

this는 매개변수의 이름과 멤버 변수의 이름이 같을 때 객체 스스로 주소를 리턴할때 사용 멤버 함수가 아니면 못쓰고 static 멤버 함수에서는 객체가 생기기 전이라 못씀  
